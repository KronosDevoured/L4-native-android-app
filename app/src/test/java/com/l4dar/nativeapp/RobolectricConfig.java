package com.l4dar.nativeapp;

import org.robolectric.annotation.Config;

@Config(manifest = Config.NONE)
public class RobolectricConfig {
    // Empty - just provides base configuration for all Robolectric tests
}
